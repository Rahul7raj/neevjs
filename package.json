{
  "name": "neevjs",
  "private": true,
  "version": "0.0.1",
  "description": "NeevJS — Plugin-driven, offline-first React framework for business apps",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter demo dev",
    "dev:server": "pnpm --filter @neevjs/server dev",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
